pipeline {
    agent {
        docker {
            image 'golang:1.24'
        }
    }

    stages {
        stage('Unit Tests') {
            steps {
                dir('bugtracker-backend') {
                    sh 'go version'
                    sh 'go test -v ./...'
                }
            }
        }
    }
}