pipeline {
    agent any

    stages {
        stage('Unit tests - backend') {
            steps {
                dir('/Users/dhruvgoel/bug-tracker/bugtracker-backend')
                go test -v ./...
            }
        }
    }
}
